This branch is `codex/dc-training`.

Purpose:

- hold the canonical full runnable DC training bundle
- keep the DC-specific H200 smoke / pilot / extended command surface alongside the training flow
- serve as the branch where full DC experiments, reports, and trained artifacts belong

This branch intentionally keeps the normal DC training files so the H200
commands run against the real DC training flow.
