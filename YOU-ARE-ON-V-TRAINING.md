This branch is `codex/v-training`.

Purpose:

- hold the canonical full runnable V training bundle
- keep the V-specific H200 smoke / pilot / extended command surface alongside the training flow
- serve as the branch where full V experiments, reports, and trained artifacts belong

This branch intentionally keeps the normal V training files so the H200
commands run against the real V training flow.
